// const sortDataByPrice = (data, arrange) => {
//   if (arrange === 'async') {
//     return data.rows.sort((a, b) => a.price - b.price);
//   } if (arrange === 'desc') {
//     return data.rows.sort((a, b) => b.price - a.price);
//   }
//   return data.rows;
// };
// module.exports = sortDataByPrice;

// const sortDataByPrice = (data, arrange) => {
//     if (arrange === 'async') {
//       return [...data.rows].sort((a, b) => a.price - b.price);
//     } if (arrange === 'desc') {
//       return [...data.rows].sort((a, b) => b.price - a.price);
//     }
//   };
//   module.exports = sortDataByPrice;
